
  <h1><%=@task.title%></h1>
  <h2><%=@task.details%></h2>
  <h3> <%= link_to "Edit a task", edit_task_path(@task) %></h3>
  <h3><%= link_to "Delete a task", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></h3>
  <h3><%= link_to "Tasks list", tasks_path %></h3>
